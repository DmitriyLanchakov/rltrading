library(dplyr)
library(tidyr)

function obTime(dfall,badtime){}
#     tt<-first(filter(dfall, datetime==badtime)$datetimebroker)
#     dftt<-filter(df,datetimebroker==badtime)
#     dtF<-first(dftt)$datetime
#     dtL<-last(dftt)$datetime
#     tt+as.numeric(difftime(badtime,dtF, units="secs"))/as.numeric(difftime(dtL, dtF, units="secs"))
#     
}
